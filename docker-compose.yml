#version: '3.8'

services:
  infraatlas:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: infraatlas
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
