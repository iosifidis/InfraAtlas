version: '3.8'

services:
  vm-dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vm-dashboard
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
